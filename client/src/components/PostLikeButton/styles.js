import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    align-items: center;
    margin-right: 3.6rem;

    .icon {
        color: var(--primary-color);
        margin-right: 1rem;
        cursor: pointer;
    }
`;
