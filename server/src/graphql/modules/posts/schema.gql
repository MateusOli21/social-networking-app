type Post {
  id: ID!
  body: String!
  username: String!
  createdAt: String
}

type Query {
  getPosts: [Post]
  getPost(id: ID!): Post
}

type Mutation {
  createPost(body: String!): Post!
  deletePost(id: ID!): String
}
